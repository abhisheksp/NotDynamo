apiVersion: v1
kind: Service
metadata:
  name: notdynamo-data-headless
spec:
  clusterIP: None
  selector:
    app: notdynamo-data
  ports:
    - name: grpc
      port: 9090
      targetPort: grpc
    - name: http
      port: 8080
      targetPort: http
