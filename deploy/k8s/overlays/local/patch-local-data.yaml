apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: notdynamo-data
spec:
  replicas: 1
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        resources:
          requests:
            storage: 5Gi
